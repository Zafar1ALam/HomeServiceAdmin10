import React from 'react';
import PropTypes from 'prop-types';
import Text from 'react-native'
const a = props => {
    return (
        <Text>fffffffff</Text>
    );
};

a.propTypes = {

};

export default a;